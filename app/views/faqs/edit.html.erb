<h1>Edit FAQ</h1>

<%= form_for(@faq) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit 'Update' %>
  </div>
<% end %>
